[build-system]
requires = [
    "setuptools>=61.0.0",
    "scikit-build>=0.13",
    "cmake>=3.18",
    "ninja; platform_system!='Windows'",
    "cython",
    "numpy>=1.26.4"
]
build-backend = "setuptools.build_meta"
